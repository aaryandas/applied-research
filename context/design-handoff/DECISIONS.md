# Architecture and immediate decision gates

September 8 execution decisions in [AR-28](https://linear.app/aaryan-das/issue/AR-28): React Flow, React Aria composite controls, native plain-text human writing and maintained Markdown/math/HTML/PDF parsers are approved as needed. Stable Drizzle + better-sqlite3 is selected locally, with meaningful content revision history rather than universal events. PostgreSQL + Drizzle serves backend account/session/usage. See [the workbench contract](../workbench-contract.md); exact package/ABI/migration verification remains implementation work. No hosting purchase, sync or embedding service is authorized by these choices.

## Latest founder delegation · September 8

Subsequent explicit approvals: [AR-12](https://linear.app/aaryan-das/issue/AR-12) uses Better Auth seven-day rolling sessions, renewal after one day, database session validation on every paid AI request, online revocation and immediate local credential clearing at sign-out even offline. [AR-14](https://linear.app/aaryan-das/issue/AR-14) uses Manim Community 0.21 original reviewed recipes in an isolated backend worker, with Three.js/React Three Fiber locally. 3b1b scenes remain quality references, not reused product assets. Local container verification is allowed; deployment and additional paid render capacity remain separate.

The founder authorizes agents to determine and implement Settings/sign-in, Practical Work, Manim/Three.js and companion designs from the established design system, patterns and discussions, without another per-screen reference approval. Clicky is the companion usefulness/behavior standard; [3b1b/videos](https://github.com/3b1b/videos) and 3Blue1Brown videos are the Manim quality standard. Agents must derive concrete criteria, implement real behavior and obtain independent review. This supersedes earlier missing-reference/visual-approval gates for those surfaces, not safety, spending, disputed foundations or final product acceptance. Playbook remains deferred until the rest is built out; the teammate argument graph remains roadmap work. See [the updated gauntlet](GAUNTLET-PROMPT.md) for the authoritative execution instructions and quality criteria.

The founder can refresh reviewer login and expects it to work now; the next session must test access rather than assuming either the prior failure or recovery. The founder increased the concurrency cap to seven active workers, including reviewers, during the September 8 build-speed audit; see [the current orchestration policy](SUPERSET-ORCHESTRATION.md#throughput-policy--september-8-audit). Keep the existing cumulative $2/10-request authorization.

## Accepted constraints

Build kickoff decisions, 2026-09-08 UTC: [AR-5](https://linear.app/aaryan-das/issue/AR-5) requires assembled screen-by-screen approval before dependent visual implementation. [AR-7](https://linear.app/aaryan-das/issue/AR-7) authorizes at most **US$2 total and 10 live provider requests** with isolated synthetic topics, after configured-model and secure-credential checks. This does not set production usage limits or authorize infrastructure spending. The running development app reported unconfigured provider status at preflight; no live call has been made. [AR-13](https://linear.app/aaryan-das/issue/AR-13) owns the full-app plan and its dependency-linked coverage tickets.

The founder's latest scope decision is the **full desktop application**, not an MVP stopping point. All product screens and reusable components, interface animation, Manim explanations, Three.js interactive experiences, embedded browser and Clicky-like companion are explicitly in scope. MVP priorities only sequence delivery. [Full-app target](../full-app.md) owns the coverage and completion requirements; AR-5 records the decision. Detailed unresolved behavior still requires founder decisions.

AR-12 selects app-managed AI through our backend: users sign in to Applied Research and never supply the company provider key. Replace key-file import before production acceptance. [Credential ownership and implementation gates](../credentials.md) govern the replacement; hosting/auth/budget choices remain open.

Later CI-session decisions: Effect v3 is selected for the next backend work; add it with that work, without migrating the backend in the CI task. Resolve the test adapter/version compatibility at adoption ([testing guidance](../testing.md#effect-adoption-gate), AR-6). SonarQube was local-only under AR-9; [AR-45](https://linear.app/aaryan-das/issue/AR-45) (2026-09-09) supersedes that for GitHub analysis, which now runs post-merge on `main` against the founder's self-hosted Railway Community Build instance because the local instance is unreachable from CI. PR diffs are never scanned into the hosted project and paid/public SonarCloud hosting remains unselected. Main requires an up-to-date CI gate and a pull request, with no mandatory second reviewer (AR-8).

Local authority owns learning records, observation scope and desktop actions; bounded remote AI/compute returns validated results. The [authority diagram](../diagrams/local-authority.html) shows logical responsibilities, not a chosen deployment/process count. Read [architecture](../architecture.md), [active presearch](../presearch.md) and the [decision audit](../decision-audit.md) together.

Maintain Electron main/preload/renderer separation, sandboxing, context isolation, denied permissions, restricted navigation, untrusted guest isolation and named validated bridge commands. Credentials/SQL/arbitrary filesystem access never enter the renderer. An AI contribution never silently becomes human writing; citations and experimental evidence are different provenance. Ordinary cursor movement does not authorize observation. Guidance requires an explicit start and ends on stop/completion. Saved local learning work must be readable and editable offline.

OpenRouter was explicitly selected for the current MVP. Reusable explanation capabilities with supported fallback were selected over arbitrary generated-code execution. Durable-task recovery hardening was deferred, not approved. Basic saving, isolation, validation and truthful failure behavior remain required.

## September 8 foundation and worker decisions

The founder selected **Better Auth with GitHub sign-in instead of Auth0**, agreed to the immediate recommendations of **Railway** and **Effect 3.22.1 with existing Vitest 4 and public Effect test services**, and initially authorized two concurrent Superset workers. The later September 8 build-speed decision increases that limit to **seven concurrent Superset workers**, including reviewers. This supersedes the pending host/auth/concurrency recommendations, not all options in the research documents. Do not install the incompatible `@effect/vitest` adapter. Existing Sol/Astra implementation and Fable 5.1 High independent review roles remain. Record exact supported auth dependency versions and Electron integration requirements before adoption.

Hosting region, account/domain configuration, session policy and production usage/infrastructure budgets still require concrete decisions before dependent deployment or paid operation. The bounded $2/10-request development validation budget is unchanged. The founder separately approved the revised Opening layout with “Yes” on September 8 after viewing the project-discoverability correction. This clears its visual gate only; the other assembled screens await approval.

## September 8 Learning Path correction

The founder rejected the standalone Learning Path screen. AR-16 now owns a persistent, expandable topic/concept sidebar beside the active Reader or practical content, following the supplied paper-topic and Heptabase structural references. The path capability remains in scope; its separate screen/navigation entry is superseded. The revised sidebar/content assembly requires review, while Opening retains its visual approval.

## Current implementation, not new decisions

Node24/npm, Electron/React/TypeScript/electron-vite, direct main-process OpenRouter calls, named preload commands, local `node:sqlite` project snapshots, safeStorage key import, an isolated guest and a matrix capability are implemented. Read [MVP status](../mvp.md) and [code map](../code-map.md). The assembled design has not been ported wholesale to Electron.

Do not rewrite the installed stack just because an older record names another choice. Equally, do not dismiss prior Effect/Drizzle/event-model discussions because they are not installed. [AR-6](https://linear.app/aaryan-das/issue/AR-6/decision-reconcile-the-mvp-architecture-before-adding-foundational) requires a researched recommendation and founder decision for affected foundations. Follow the existing npm workflow until a package-manager change is explicitly approved. License and distribution conflicts must be resolved before affected release work.

## Gates to raise immediately

| Gate                                                                                                                              | Present status                                                                                              | Recommendation / options / unblocks                                                                                                                   |
| --------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| [AR-5: full-app scope](https://linear.app/aaryan-das/issue/AR-5)                                                                  | Confirmed: full desktop app, including animation/Manim/Three.js, browser and companion.                     | Map all agreed requirements to delivery tickets; MVP priorities sequence work and cannot limit final completion.                                      |
| AR-5: design baseline                                                                                                             | Per-screen assembled approval selected; revised Opening approved.                                           | Obtain acceptance of each revised assembled screen before its production port. Does not approve unresolved behavior by inference.                     |
| [AR-6: architecture](https://linear.app/aaryan-das/issue/AR-6)                                                                    | Effect v3 and backend-managed AI are selected; remaining foundations need decisions.                        | Resolve hosting/auth/rendering placement and other affected choices with researched options.                                                          |
| [AR-7: preflight](https://linear.app/aaryan-das/issue/AR-7/preflight-verify-build-access-openrouter-configuration-and-validation) | Linear connected. OpenRouter configured status/live success not checked in this handoff.                    | Check named provider-status operation or settings; use trusted environment/native key import if absent. Never paste keys into tickets or chat.        |
| Live validation                                                                                                                   | Model/search capability, spend cap and representative topics not yet approved for this build.               | Present a bounded live-test plan and actual prerequisites before paid runs. Recorded fixtures cannot establish live latency/citation quality.         |
| Delivery targets                                                                                                                  | Desktop-first is selected; exact next-build OS/window acceptance and distribution target need confirmation. | Propose local Electron acceptance first or signed multi-platform delivery with its required access. Do not assume publication.                        |
| Surface contracts                                                                                                                 | Several rows in the design contract remain proposals.                                                       | Create a decision issue only for the next affected slice; show concrete UX/API/data examples and recommend a choice. Continue independent ready work. |

The first useful-step target is under 20 seconds; short-clip and longer-clip waits are distinct. Roughly 1,000 concurrent users is a stated requirement, not a demonstrated capacity. Scope a meaningful validation plan; never convert an unverified requirement to a completed acceptance claim. The full-app coverage plan must reassess previous MVP deferrals; do not treat current missing implementation as an exclusion. Hosting/auth/rendering choices needed by approved capabilities require decisions. Sync, billing, voice and arbitrary automation are not implicitly selected by the full-app scope correction.

No recommendation in this file is a decision. Record the founder’s chosen option, rationale, date, source, affected tickets and what it supersedes in Linear; then update the relevant repository context.

## Gauntlet readiness clarification · September 8

No further general-purpose mockup is required before backend specification begins. Opening, Reader notes-first interaction and Canvas have recorded scoped approvals. The approved graph direction establishes shared topic/chapter → notes/questions → insights and exact source provenance. The portable specimen is visual/interaction evidence, not the production data model or graph engine.

Remaining reference work is bounded: Settings/sign-in/session/quota states; explicit Practical Work acceptance and supported tool/result states; representative Manim/Three.js/companion interactions before those capability slices. Existing examples can be assembled and reviewed per slice. Playbook is founder-deferred until the rest is built out; the teammate argument graph remains roadmap work.

Before dependent backend coding, settle the concrete local-record/source-version/link schema and migration, API/result contracts, remaining auth/session and usage policy, and affected maintained-library choices through AR-6/AR-12/AR-28. Railway, Better Auth/GitHub, Effect v3 and OpenRouter are already selected and should not be reopened as missing references. Separate runtime access and independent reviewer availability from visual readiness; the last observed Fable session had expired login. No renewed runtime preflight is claimed by this documentation check.

Backend specification and other independent ready work can proceed. A complete build → independent critique → repair → integrate cycle needs a ready slice and functioning reviewer; unresolved screen references block only the affected visual implementation.
