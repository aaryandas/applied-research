# Applied Research — full application implementation gauntlet

Use this prompt in a new implementation session in this repository, including the current uncommitted handoff and application work. The founder has authorized agents to complete the remaining reference/design work specified below. Start the gauntlet in that session; editing this file does not start it. The loop is the active build/review/repair cycle, not an installed scheduler or a claim of work continuing after the session stops.

---

Build the complete Applied Research desktop application to the exact product, architecture and design direction I have approved. The target is the full app; MVP priorities only order the work and are not the stopping point. Finish the actual working product, not another mockup. Use a demanding gauntlet: parallel implementation workers, independent visual and functional critics, explicit evidence, and repeated fixes until every approved acceptance criterion is satisfied. Resolve the delegated design and interaction details using the established product direction, references and sound implementation judgment. I retain decisions on material changes outside that delegation; do not reopen choices already settled or halt for routine details.

Start at `AGENTS.md` and `context/design-handoff/README.md`. Read `DESIGN-CONTRACT.md`, `DECISIONS.md`, `LINEAR-WORKFLOW.md`, the portable reference in `context/design-handoff/prototype/`, `context/presearch.md`, `context/product.md`, `context/domain.md`, `context/architecture.md`, `context/diagrams/local-authority.md`, `context/mvp.md`, `context/code-map.md` and relevant parts of `context/decision-audit.md`. Read historical sources only for a specific conflict. Do not load the vendored Effect tree as application code.

Latest founder sequencing correction (September 8): defer Playbook redesign/implementation until the rest is built out. Its current reference is rejected. Prioritize backend assembly and approved surfaces; this deferral is not a whole-build blocker. Opening, the recorded Reader notes-first flow and Canvas have scoped acceptance receipts in DESIGN-CONTRACT.md. The latest delegation below supersedes earlier per-screen approval gates for the named remaining surfaces; do not wait for more mockup approvals before building them.

## Unattended execution objective

The founder intends to leave this session running while asleep and return to a completed, usable application for the active non-deferred scope. Optimize for integrated working behavior and verified delivery, not more prototypes, long research reports or an expanding backlog. This is the execution target, not permission to claim success without evidence or a guaranteed deadline.

- Continue autonomously through implementation, testing, independent review, repair and integration. Do not end the run after planning, a worker dispatch, an isolated passing slice or a status report while ready work remains.
- Resolve routine implementation details and delegated designs yourself, record the decisions briefly, and keep moving. Reuse the existing architecture research instead of restarting broad comparisons. Prepare only the contracts needed to unblock the next slice.
- Surface genuinely external blockers promptly, but do not wait idle for the sleeping founder when independent work is available. Complete all work that does not depend on the missing credential, account, unresolved foundational decision or additional spending. Never treat silence as approval or hide an incomplete integration behind a mock.
- Preserve the seven-worker limit and all existing credential, permission and cumulative spending boundaries. Check reviewer access early so a login failure does not first appear at final handoff. Do not create recurring automations or claim the run continues after the session stops.
- Keep integrated checkpoints and concise Linear receipts as you go, including exact source revision, completed slices, current failures and the next action. Preserve existing uncommitted work; do not require the founder to reconstruct worker changes in the morning.
- At handoff, leave the real app runnable and provide its launch command/build location, the implemented end-to-end journey, actual automated/Electron/independent-review/Sonar results, and a short list of any remaining blockers. Clearly distinguish completed non-deferred scope from the deferred Playbook and teammate argument graph. Do not call an unverified or partially connected product finished.

## Latest founder delegation — authority for this session

The founder explicitly authorizes you to determine and implement **Settings/sign-in, Practical Work, Manim/Three.js experiences and the companion** from the design system, patterns and discussions already recorded. This includes their normal, loading, empty, error, offline, canceled and retry states. Specify these details, record the rationale and acceptance criteria in Linear, then build and independently review them. Do not ask the founder for another reference, an assembled-screen approval, or routine layout/interaction choices for these areas. Missing reference states are work for you to complete inside the gauntlet, not a reason to stop it. This delegation overrides older contrary reference/approval language in linked context and tickets for these areas; it does not retroactively approve the rejected Playbook.

Use these standards:

- **Settings/sign-in:** the accepted Applied Research typography, palette, profile placement and interaction patterns; Better Auth with GitHub sign-in, through the supported system-browser desktop flow. Design session-expiry, sign-out and quota feedback consistently without exposing credentials or blocking access to saved offline work.
- **Practical Work:** the discussed embedded-tool/external fallback, explicit guidance, result-return and reflection flow. Preserve useful context between learning, tools, sources and Canvas; implement real supported behavior rather than the illustrative wheel-speed fixture.
- **Companion:** Clicky is the standard for both behavior and usefulness, as discussed in the recorded references. Judge it on actual contextual help, accurate pointing, the ability to follow a requested activity, and clear start/stop behavior. A cursor ornament or generic chat bubble does not meet that standard. Preserve app-scoped observation and permission boundaries; do not infer outside-app control or continuous observation.
- **Manim:** 3Blue1Brown’s style and videos are the quality reference, specifically [3b1b/videos](https://github.com/3b1b/videos). Derive review criteria from relevant scenes: mathematical correctness, carefully staged explanations, visual continuity between steps, meaningful transformations, consistent visual encoding, readable notation and deliberate pacing. Verify generated playback, not only scene code or a screenshot. The repository uses 3b1b’s Manim workflow and notes version differences from the community edition; verify compatibility before selecting/reusing components. Treat its scenes/videos as reference material, not automatically redistributable product assets; the scene repository’s license differs from the Manim library. Generate original explanations through the approved reusable capabilities, not arbitrary generated-code execution.
- **Three.js:** implement the previously discussed interactive spatial explanations and reusable simulations with meaningful manipulation, clear feedback, readable labels and supported fallback. The existing matrix exercise does not satisfy this scope; use actual runtime evidence for the agreed capabilities.

**Already selected:** Railway; Better Auth + GitHub (not Auth0); OpenRouter; Effect v3 (approved 3.22.1 adoption proposal with existing Vitest 4/public Effect test services, subject to compatibility verification); a maximum of **seven concurrent Superset workers**, including reviewers, following the founder’s September 8 build-speed increase from two. Do not install the incompatible `@effect/vitest` adapter or ask for these selections again.

**Live-test authorization:** at most **US$2 total and 10 provider requests**, on isolated synthetic topics after model/credential checks. This is a cumulative cap across sessions, not a fresh allowance. Check AR-7 and usage receipts for remaining allowance before calls; do not invent a new budget. Production usage limits, hosting purchases and additional paid rendering remain separate decisions.

**Reviewer access:** the last observed Fable session had expired login. The founder says they can refresh it and expects it to work now. Recheck with one bounded real reviewer launch during preflight, confirming the selected model, effort and tools. Do not carry the old login failure forward as an assumed blocker, and do not claim recovery without testing. If it still fails, report the actual error and continue independent ready work; never substitute a self-review or silently waive independent review.

**Still deferred:** Playbook redesign/implementation until the rest is built out; the teammate’s paper argument graph until its interface is available. Keep their records and dependencies visible. They do not block the active scope and their existing samples are not production acceptance evidence.

## Resolve authority and unblock me immediately

Use the existing Linear project **applied-research**, team **AR**. Read AR-4 through AR-7 and their current answers before starting. AR-5 records the full-app scope, accepted visual receipts and latest delegated-design authority; AR-6 owns unresolved architecture choices; AR-7 owns preflight. An unanswered gate blocks dependent implementation, not useful independent research. The delivery target includes Opening/onboarding, a persistent Learning Path topic/concept sidebar, Reader, Canvas, Practical Work, Playbook and Settings, all reusable components, and the approved app-managed AI backend/sign-in. The September 8 founder correction removes the standalone Learning Path screen; preserve the path as sidebar navigation beside content. Read `context/full-app.md`. Do not ask again whether I want only an MVP, narrow the target to currently implemented screens, or stop after the first end-to-end slice.

Immediately surface every missing API key, account access, unsupported capability, unresolved requirement, conflicting decision, test environment failure or other blocker in this conversation **and** Linear. Give me the issue, impact, recommendation, 2–3 viable options when applicable, and the exact action that unblocks you. Ask early, while other work can proceed. Do not collect blockers silently until the end. Do not treat silence or elapsed time as approval.

First inspect repository state, existing work, Linear access, Node24/npm, desktop automation, the design assets, available test environments and the provider’s configured status. OpenRouter is selected; do not arbitrarily switch providers or model. Check supported live API/model/search behavior against current official documentation. Use the remaining authorized cumulative live-test budget above. The old native key-file import/safeStorage flow is development-only; production AI verification uses backend-managed secrets and authenticated requests. Use trusted configuration; never ask me to paste keys into chat/Linear, print secrets, put them in renderer variables, commit them or send them to subagents. Report configuration status only. Do not disable security to make a missing integration appear to work.

## Follow the architecture; use maintained libraries

Read `context/credentials.md` and AR-12. App-managed AI is selected: the company OpenRouter key stays exclusively on our authenticated backend; end users sign in to Applied Research. Replace the development key-file importer as the shipped onboarding flow. Do not send a company key or secret-manager credentials to Electron, even if local encryption is available. Railway and Better Auth with GitHub sign-in are selected. Determine sign-in UI and integration mechanics from supported patterns under the delegation. Document the concrete callback/session contract; escalate only unresolved security policy, external account/domain access and production usage/spend commitments before the action that needs them. These do not justify reopening the selected host/auth stack. Validate the complete credential lifecycle and authorization boundaries using supported components; a regex change, dotenv parser or green Sonar scan does not satisfy this requirement.

Preserve local authority over saved learning work, observation scope and desktop actions, with bounded remote AI/compute and validated returned results. Keep privileged operations behind narrow named and runtime-validated bridges. Preserve Electron sandboxing, context isolation, renderer networking restrictions, permission/navigation policy and untrusted guest isolation. Human writing, AI contributions, source citations and experimental evidence retain separate provenance. Offline saved work must remain available and editable. Guidance starts explicitly, stays scoped and stops explicitly; cursor movement alone does not authorize observation. No arbitrary generated-code execution.

The installed modules, `node:sqlite` snapshots and direct OpenRouter calls are implementation facts. Effect v3 is now selected for the next backend work; add it with that work and resolve the test adapter compatibility described in `context/testing.md`. Drizzle and event-log records still require reconciliation where relevant. Do not silently retain or replace a disputed foundation. Prepare small decision proposals per concern, with concrete interfaces/data examples where useful, and obtain my decision before affected code. Preserve saving and data integrity. Reassess MVP-era hardening and other deferrals against the full-app production requirements; present required work and unresolved choices explicitly instead of treating a temporary deferral as final acceptance. The selected host/auth stack is fixed; resolve only its genuinely outstanding deployment and security-policy choices. Additional sync, billing, voice, telemetry, licensing or distribution commitments still require approved tickets and applicable authorization.

Before building a substantial capability, inspect existing code and maintained external solutions. Prefer a supported library or platform primitive when it already solves the problem. Do not hand-roll graph interaction, rich text, parsing, math rendering, accessible widgets, orchestration or similar infrastructure just because the prototype demonstrates it with custom JavaScript. Compare suitable options using current official docs/repositories: fit to our behavior and visual requirements, maintenance, license, accessibility, Electron compatibility, performance, bundle/runtime cost, testing and migration. Recommend the smallest adequate choice and put the decision in Linear for me. A bespoke exception needs a concrete reason and my approval; “more control” alone is insufficient. Native primitives and focused domain glue are not excuses to recreate an existing subsystem. Do not add speculative dependencies. Use exact approved versions and the committed lockfile.

Do not interrupt me over variable names, formatting or mechanical details already governed by approved conventions. Make and document the delegated behavior/layout decisions without another approval. Escalate material departures from agreed behavior or visual direction, unresolved foundational dependency/storage choices, privacy/permission changes, spending and product-scope changes. An ordinary implementation detail within an agreed contract is not a new founder decision. Separate a recommendation from my decision and document both.

## Every unit of work goes through Linear

The full application explicitly includes polished interface animation, Manim-rendered explanations, interactive Three.js experiences, the embedded browser and the Clicky-like cursor-following/context-aware companion. Read the capability requirements in `context/full-app.md` and recover the associated discussions and references. Create dedicated implementation and acceptance tickets for each; these are required deliverables, not optional post-MVP work. Verify real render/playback and 3D interaction flows, embedded-tool guidance/result return, and the companion's explicit observation/start/stop behavior. Do not substitute static demos, the existing matrix exercise or a decorative character for the requested capabilities. Derive the supported interactions and representative acceptance scenarios from the recorded discussions and the standards above. Bring only unresolved execution-foundation, cost or permission choices outside the delegation to me with recommendations; preserve the approved reusable-capability and observation boundaries.

Create or reuse one parent plan for the full application, with ordered delivery milestones, then dependency-linked tickets covering the real slices and their shared components. Every substantial research, decision, implementation, verification and integration task belongs to a ticket. Search before creating duplicates. Each ticket names its user outcome, exact scope, approved decisions, reference, dependencies, acceptance criteria and evidence. Keep descriptions concise; include small code snippets only when they clarify a contract.

Use the actual team workflow. Do not invent statuses. Track blockers with issue relations and a clear status paragraph if no Blocked state exists. Record my decisions with date, rationale, consequences and superseded references, then mirror accepted contracts into `context/` with Linear links. Never bury an unanswered question under implementation progress. If Linear access fails, immediately tell me and preserve an unposted draft; pause new dependent work until I unblock it.

## Fan out bounded workers and independent critics

Coordinate Fable agents and other approved model-backed implementers/reviewers **through Superset**. Read `context/design-handoff/SUPERSET-ORCHESTRATION.md` and the available `superset-orchestrate` skill before dispatch. Earlier Superset discovery and sign-in checks are historical and must be refreshed in this session; the Claude preset inherits `fable[1m]` at High, and `codex-sol-high` explicitly selects `gpt-5.6-sol` at High. The founder has selected **gpt-5.6-sol High and gpt-6-astra for implementation, with Fable 5.1 High independently reviewing both**, with the existing build task coordinating. Assign Astra bounded visual/interaction/motion, Manim and Three.js implementation tickets, with Sol High handling other approved implementation work. Verify Astra through supported Superset configuration and preserve already-selected effort. Adding Astra to the pool does not increase approved concurrency; rotate roles within the bound. This supersedes the reversed proposal and Sol-only implementation allocation; do not ask again which model fills each role. The maximum is seven concurrent workers total, including reviewers (founder increase during the September 8 build-speed audit); read AR-7 for current runtime evidence. The inherited `fable[1m]` alias does not establish Fable 5.1: resolve the supported exact identifier, configure the reviewer through supported controls without changing unrelated global settings, and verify runtime model identity and High effort. Recheck host/workspace access and tool capabilities. Configuration is not live launch evidence; surface actual version/access/quota blockers immediately rather than substituting another release/model. Do not invent Fable/model flags or substitute generic local subagents while claiming Superset cross-model coordination is active.

Act as coordinator. Split ready tickets across isolated Superset workspaces/branches with clear file/module ownership and shared contracts. Establish an exact source handoff that includes the intended existing uncommitted work before creating worker branches; do not assume it exists on the default branch. Stay within approved concurrency limits. Suggested lanes, only after dependencies are ready: shared design/components; learning/source/Reader; Canvas and local records; practical-tool/guidance/result flow; Settings/sign-in; integration and verification. Keep Playbook outside the active lanes while deferred. Do not parallelize conflicting edits or fabricate APIs to keep workers busy. Prototype review controls, hardcoded robotics content and temporary storage are not product features.

Use Astra as an additional visual critic for Sol-authored interfaces, motion and visual explanations where useful, in fresh context with actual matched image/motion/interaction evidence. Fable 5.1 High remains the required independent reviewer of both Sol and Astra implementations. Astra cannot supply the independent acceptance verdict for its own implementation; its supplementary critique does not replace Fable review. Keep additional review sessions within the approved concurrency bound.

Give each worker the relevant issue, approved decisions, exact reference version, test data and acceptance checks. They should reuse shared components and report commit/diff, evidence, blockers and limitations using the Superset worker envelopes. Preserve ticket-to-workspace/host/terminal/model mappings in the Linear parent plan; use supported terminal read/send commands to monitor and route repairs. An envelope or idle terminal alone cannot establish acceptance. A worker never signs off its own implementation. Use independent critics in fresh sessions with a **different underlying model** from the author; give them the specification and evidence, not the implementer's praise. Verify visual critics can inspect actual image/motion evidence. Surface model/tool unavailability and reviewer disagreements instead of silently downgrading review. Reserve integration time and serialize shared desktop interactions, integration and Sonar scans. Verify combined behavior, not just isolated branches.

## The gauntlet loop

Run this as an overlapping pipeline across independent tickets, following the [throughput policy](SUPERSET-ORCHESTRATION.md#throughput-policy--september-8-audit). Dispatch ready work before waiting on integration. A reviewed contract checkpoint can release consumer implementation before the whole producer ticket is accepted; connected integration and Done still require the real reviewed producer. Code review can overlap tests and scans on the same frozen candidate; supply all resulting evidence before the final verdict. Preserve the approved worker limit, exact model roles, every required gate and single ownership of integration, shared desktop access and Sonar.

Read `context/testing.md`, `context/sonar-local.md`, `context/conventions.md` and `context/effect.md` before the first implementation cycle. Apply the available Clean Code and Patterns skill when editing or reviewing code. AR-8 owns CI integration, AR-9 records the local Sonar installation and AR-11 holds the existing maintainability backlog. Inspect their current state; do not infer that a local passing run was published to GitHub.

For every ready ticket, repeat:

1. **Specify:** translate my accepted decision into observable criteria and relevant empty/loading/error/offline/restart/accessibility states. Fill in delegated design/interaction details yourself, record observable criteria and proceed. Ask only for material decisions outside the delegation; keep independent ready work moving.
2. **Build:** implement the complete approved slice in the real Electron app, using the approved shared components and libraries. Preserve unrelated work and actual user data. No dead controls, fake provider success, mock-only flows or hidden data loss.
3. **Exercise:** run appropriate automated checks and the actual user journey in Electron with isolated synthetic test data. Test source/AI provenance, cancellation, guest fallback, persistence and export where applicable. Use live provider checks only within authorized credentials and spend; label recorded fixtures honestly.
4. **Compare:** capture the reference and implementation side by side with matched viewport, zoom, theme, content and state. Include default, long/short content and the states affected by the ticket. Where useful, randomize A/B labels for the critic’s first visual pass and disclose the mapping afterward; never claim a blind comparison if the identities were visible. For delegated surfaces without a frozen screen, compare against shared tokens/patterns and the documented scenario criteria. For Manim and the companion, also evaluate the reference-specific quality/usefulness criteria above. Judge fidelity and usability against those standards, not an unrelated aesthetic or personal taste.
5. **Critique independently:** a visual critic checks hierarchy, exact typography/spacing, surfaces, node identity, motion, focus and overflow; a functional/architecture critic checks real behavior, component reuse, contracts, provenance, isolation and failure recovery. The independent code reviewer also applies the TypeScript/Effect standards below and reviews the actual diff and Sonar findings. Require concrete evidence-linked findings and a pass/fail verdict for each criterion. “Wow”, a score, a green build or a polished screenshot alone cannot pass the ticket.
6. **Repair:** record findings in Linear, fix the whole relevant batch, rerun affected checks and recapture the same states. Return to the same critic for resolved/partial/unresolved findings. Repeat while concrete approved requirements remain unmet. Do not introduce new aesthetic preferences or scope under cover of critique.
7. **Integrate and scan:** merge the reviewed slice into the integration branch only after its dependencies are ready; repeat the cross-surface journey and regression checks, then run the Sonar cycle below on that exact combined source. Route findings back through repair and independent review. Mark Done only when integrated acceptance evidence, applicable quality gates and independent reviews pass.

Keep working while there is approved, unblocked work. If a loop stalls on the same cause, findings do not improve, or the necessary fix changes a decision, tell me immediately with evidence and options; pause that dependency, not the whole project. Do not pretend perfection, weaken a test, suppress a failure, endlessly churn the design or spend without bounds to satisfy an adjective. Resume the ticket after I unblock it. Do not silently stop with material open findings.

## Sonar cycle — required throughout implementation

The coordinator owns scans because the local Sonar project has one shared analysis history. Serialize scans across workers and record which integration revision is being analyzed. Never let simultaneous worktrees overwrite each other's results or treat a scan of another checkout as evidence. Stop edits to the scanned source until that analysis finishes. Record the commit and any uncommitted changes; a dirty checkout must not be represented as the committed revision.

For each integrated application-code slice, after a repair that changes analyzed code, and before final acceptance:

1. Run `npm run check` and the applicable real Electron/package tests. Start the approved local stack with `npm run sonar:start` if necessary and verify readiness. Use `npm run sonar:scan` from the intended checkout; it regenerates coverage and waits for the quality gate. An unreachable server, expired token or failed scanner is a blocker, not a skipped pass. Surface it immediately and continue only independent work. Credentials stay in the existing ignored local configuration; never give them to reviewers or include them in evidence.
2. Read the resulting issues and quality-gate conditions through the local dashboard or supported Sonar API. Inspect new issues and existing issues in changed code, including reliability, security and maintainability findings. Record the analysis identifier/time, source revision, actual gate conditions and issue keys/rules/locations in the owning Linear ticket. A green gate alone does not establish zero findings, adequate coverage or correct TypeScript/Effect behavior.
3. Fix introduced findings and material findings affecting the ticket, then rerun affected checks and rescan. Have the independent reviewer verify both the code and the finding resolutions. Do not mark a false positive, accept a risk, add an exclusion, disable a rule or relax a gate without presenting the evidence and obtaining my decision. Do not mechanically apply a rule suggestion that would break Electron's runtime or bundler constraints.
4. Keep pre-existing unrelated findings linked to AR-11 or an appropriate follow-up. Do not silently absorb the entire backlog into every feature, erase it, or claim it was fixed. If an existing finding compromises the slice's correctness or architecture, make it an explicit dependency and surface any resulting scope decision.

Local Sonar does not run from hosted GitHub Actions and does not provide native PR analysis in this setup. GitHub's required `checks / CI gate` remains a separate gate. Do not add a background watcher, auto-merge, public Sonar hosting or a scheduler by inference. These scans happen as actions of the running implementation task; if the task stops, the loop is not running.

## TypeScript and Effect standards — independent review required

Review standards and specification compliance separately. The reviewer reads the actual changed modules and their callers, rather than relying solely on lint, coverage percentages or the implementer's summary. Cite the relevant repository rule and concrete code location for each finding.

- **TypeScript:** preserve strict checking across main, preload, renderer and tests. Validate unknown external input at runtime before using it. Model domain states explicitly, handle variants exhaustively, and maintain focused modules with useful typed interfaces. Do not use `any`, unjustified assertions/non-null assertions, blanket error swallowing, `@ts-ignore` or disabled lint rules to hide a problem. Handle promises and cancellation deliberately; preserve useful error information without leaking credentials. Prefer existing supported primitives and modules over speculative abstraction or duplicate infrastructure.
- **Effect v3, when adopted:** keep the approved runtime/service boundaries and exact compatible versions. Use typed error channels, testable services/Layers, scoped resource acquisition/finalization and interruption for work that needs them. Own and dispose the runtime at the application composition boundary; do not scatter unowned runtimes or detached fibers through domain code. Keep serializable validated data at Electron IPC boundaries, never Effect runtime/service objects. Use v3 documentation and the pinned reference selectively; do not mix prerelease v4 APIs or import vendored code.
- **Effect tests:** verify success, typed failure, interruption/cancellation and resource cleanup for implemented behavior. Prefer Effect's TestClock and test Layers for deterministic time and dependencies; use real adapter and Electron tests at integration boundaries. Resolve the documented `@effect/vitest`/Vitest peer-version mismatch before installation. Do not force dependency resolution, downgrade silently or hand-roll a test framework.
- **Review completion:** fix material standards findings, rerun the relevant checks and return them to the independent reviewer. Escalate conflicting standards or changes to architecture/dependencies to me with evidence and options. A successful Sonar scan cannot waive this review.

## Visual requirements that must survive implementation

Apply `DESIGN-CONTRACT.md`, especially the later corrections: approved apple opening and direct input; Newsreader 22px headings/18px reading with the correct weights and optical sizing; Familjen UI/AI; Martian navigation; restrained Fraunces briefs; continuous dark ground; rounded controls and consistent icons; no structural divider grids; content-first Canvas with distinctive node types, high-contrast actual text and no filler headings/action footers; horizontal curved relationships; insights derived from saved notes/questions; Playbook remains deferred. Preserve the approved infinite dotted Canvas, topic/chapter origin links, Distilled/Expanded views, automatic icon rail and thin top bar, and restore the full topic sidebar outside Canvas. The latest specific correction overrides an older screenshot. Do not restore Workbench/Writing, Reader spines/path bars, generic nodes, right-angle connectors or product copy that was rejected. Use the current portable specimen and recorded approval hashes for accepted surfaces. Complete the delegated surfaces consistently from that design system; no additional global reference-approval gate is required.

Implement real shared tokens/components and maintained interaction primitives. Do not copy the prototype’s review service, hardcoded credentials, temporary state or example text into production. Preserve human content exactly. Make both themes and agreed desktop sizes work; no clipped essential content, unreadable fit-to-screen text or nonfunctional keyboard paths. Match the intended motion and respect reduced motion. A source code inspection is not a visual test.

## Final acceptance

Maintain a Linear coverage ledger for every approved product area, reusable component and cross-surface flow, including real data, error/offline states and backend integration. Complete all approved rows, not only the early MVP milestone. A rendered screen, placeholder control or mocked service is not implementation evidence. Each outstanding requirement must remain visible as unfinished or carry my explicit exclusion/deferral. Once all non-deferred scope passes, report that milestone and that Playbook is ready to revisit; do not resurrect its rejected design or call the whole product complete while deferred work remains. See `context/full-app.md` for the completion boundary.

Run repository-required checks: `npm run check`; `npm run test:e2e` for Electron/main/preload/renderer changes; packaging and packaged smoke checks if packaging changed. Use the supported Node24 toolchain and isolated test data. Do not weaken coverage, isolation, CSP or test gates. Record exact commands, runtime, results and limits in Linear. Tests against recordings do not prove live model availability, citation faithfulness, response latency or capacity.

Demonstrate the complete approved learning loop: arbitrary topic → useful sourced step → reading/explanation → practical activity or external tool → returned evidence → preserved insight/Canvas relationship → next useful learning step. Playbook compilation/export remains outside this active pass while deferred. Verify that real saved work survives the approved restart/offline scenarios and that attribution stays correct. Measure approved performance targets with a stated protocol; distinguish them from unverified requirements. Include focused input, keyboard movement, long notes, failed requests, cancellation, incompatible guest tools, empty states and export where those belong to the milestone.

Before the final handoff, reconcile all Linear tickets and decisions, clear material review findings, update the code map and owning context pages, and provide the run command, working build, test/visual/live-validation evidence and any unresolved limitations. My acceptance is separate from agent review. If an essential prerequisite is still blocked, show me the exact blocker and evidence; do not call the full application complete. Do not deploy, publish, sign, or change billing merely because the local implementation passes.

Start now with a bounded preflight and current Linear decisions, including AR-12. Prioritize the backend: capture the architecture diagram, shared record/source-version/link contracts, named desktop operations, backend request/result contracts and migration plan in the existing owning context/tickets. Use the same project/topic/chapter/source/highlight/note/question/insight identities across Reader and Canvas; separate local durable records from backend account/session/usage/provider responsibilities. Propose only genuinely unresolved foundations rather than asking for another general design interview.

Order the first implementation slice around authenticated AI access, validated generated/source data, durable local saving and the existing Reader/Canvas relationship, with errors/cancellation/authorization boundaries exercised. Split it into reviewable dependent tickets; implement ready parts as soon as their contracts are settled. Continue through build → exercise → independent critique → repair → integration/Sonar, then the remaining non-deferred scope. Do not end after presenting a plan or another prototype. If one dependency needs a founder answer, surface that precise decision and continue independent ready work. Preserve uncommitted work and do not publish or deploy it without the existing required authorization.
