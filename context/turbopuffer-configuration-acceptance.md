# Turbopuffer configuration acceptance — September 9, 2026

Operator configuration probe passed at 08:43 UTC. This is real provider/index evidence, not application retrieval, broad corpus, course generation or production acceptance. Application source authority, exclusions, idempotency and HTTP composition still require AR48's reviewed implementation and full pipeline test.

- Existing Applied Research Railway production/api configuration supplied the approved OpenRouter and turbopuffer credentials to an operator process in memory; no key was printed, saved to files, sent to a cloud agent or included here. Production `AI_ENABLED` was unchanged and no generation request ran.
- OpenRouter request `qwen/qwen3-embedding-8b`, route `deepinfra`, `dimensions:1024`, float encoding, `require_parameters:true`, no fallbacks, maximum prompt price$0.01/M and requestfee0 returned three real1024-dimensional vectors for two public paragraphs and one retrieval query.
- **Compatibility finding:** response model is `Qwen/Qwen3-Embedding-8B`, the upstream model ID, rather than the lowercased OpenRouter request ID. Preserve an explicit reviewed mapping between those exact IDs; do not loosen to arbitrary model aliases or silently truncate dimensions.
- Input source: Python Software Foundation's version3.14.7 floating-point tutorial, PSF-licensed. Its pinned rawHTML hash matched. Only two extracted public paragraphs were indexed; this operator extraction is not the application's canonical full-text pipeline.
- Oregon `aws-us-west-2` accepted vector write and strongly consistent ANN query. Expected representation-error paragraph ranked first (cosine distance0.1565842), comparison/remedy paragraph second(0.4542367). Returned text/hash matched the input snapshot exactly.
- Deleted both rows, confirmed zero rows by strong query using the same retained query vector, and purged only this run's owned namespace with HTTP200. Five physical turbopuffer requests in the successful continuation; no namespace remains from the test.
- The initial strict model-name check rejected a valid embedding response before any vector write. That diagnostic cost was retained in the same allowance. The second response was saved for inspection and reused for all vector operations; no third embedding request occurred.

## Shared initial embedding allowance (do not reset)

Original allowance ID: `ar48-initial-embedding-evaluation-2026-09-09`; original ceiling250,000 microUSD($0.25). Two physical embedding calls consumed177 tokens each and reported$0.00000177 each, total$0.00000354. Conservatively round each settlement upward: **4 microUSD consumed, 249,996 microUSD remaining**. No uncertain reservation remains. These prior operations must be imported/reconciled before AR48 creates its durable production evaluation allowance; a fresh$0.25 would double-allocate approval.

Operation IDs: `fba2defc-8bdf-4482-a779-15b7e8359449` and `e4dec66c-5cdc-47c4-84c8-bede89b7db18`. Coordinator retains exact local operator ledger and sanitized receipts; do not read workstation files from Cloud. Turbopuffer use is separate from embedding allowance: write receipt8,873 billable logical bytes; each query reports its minimum256,000,000 billed logical bytes. The $16 Launch minimum is not a hard usage ceiling.

## Remaining acceptance

AR48 must integrate the real transport/embedding identity handling and durable allowance with canonical acquisition, refreshed account/source authority, exact evidence locators, exclusion/deletion, physical retry accounting and backend routes. Use this evidence to avoid repeating setup-only embedding calls. The full pipeline probe can spend only from the remaining shared allowance and must preserve productionAI disabled until separately reviewed scoped acceptance.
