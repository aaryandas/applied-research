# Remaining steps for the surfaces map (2026-09-06)

Run in this order. Each prompt is self-contained for a fresh session in this repo.

## Step 1 · Task 06 (an agent with computer use)

Resolve ticket 06 on .scratch/surfaces/map.md by capturing the missing reference assets. Claim the ticket first (set Status: claimed). Use the superset:computer skill to drive the desktop. Open the Codex desktop app (install it from OpenAI's site if it is not installed; sign in if the app asks and stop to tell me if it needs credentials you do not have). Capture four PNG screenshots of the whole app window at native scale, no scaling, into references/chrome/codex-desktop/: empty.png (a fresh workspace with no threads or tasks), running-task.png (a task in progress, showing the running state and any progress indicator; start a trivial task like "list the files in this folder" on a scratch folder to get it), settings.png (the settings or preferences screen), long-list.png (the thread or task list with many items; if none exist, the longest list the app offers). Name states exactly as above. Then record in references/README.md, in the Codex desktop row, the four file paths, the app version, the theme in use, and the fonts if the app exposes them, and remove the "needs Aaryan's screenshots" note. The blue engraved-landscape screenshot for the World Labs row is not on this machine; leave that row's note as is and say so. Do not modify any other file. Append an Answer block to .scratch/surfaces/issues/06-capture-missing-reference-assets.md listing what was captured and what could not be, set Status: resolved, and add one line to the Decisions so far section of .scratch/surfaces/map.md. Never send any file to an external service.

## Step 2 · Prototype 17 (Canvas with the Learning Path and elevation)

/wayfinder .scratch/surfaces/map.md ticket 17

Claim ticket 17. Read the map's Decisions so far and the Answer blocks of tickets 05, 11, 12, 18, 21 in full. Run `impeccable shape` first to write a one-page brief for the Canvas at `docs/gauntlet/briefs/canvas.md`, then `/prototype`: a throwaway, single HTML file with inline CSS and JS, no framework, in the material system (dark default; Newsreader for human text and questions, Fraunces for the Brief, a grotesk for chrome and AI text; warm equals human, cold equals interface; Phosphor Light icons). Real content: Brief "I'm building a retrieval layer for a coding agent and need to understand why attention scales the way it does"; Learning Path with root, four topics, twelve lessons with mixed statuses (Not started / In progress / Completed / Mastered), Next and the edge marked; under two lessons, question cards with cited answers (citation chips "Vaswani · §3.2", "Lecture 4 · 12:40") and two notes as leaves; one insight card above two question trees from different lessons connected downward; one thesis above the insight with claim and "what would prove me wrong"; the bottom text box "What do you want to understand?" with the "Find new sources" switch; the right sidebar as one scrolling details panel for the selected node (lessons, statuses, sources). Pan and zoom must work; zoomed out the elevation must read at a glance; show the Connect gesture (select two cards, a writing field opens above them). Open it in the browser with /browse, screenshot zoomed out and zoomed in, and put both in `docs/gauntlet/prototypes/canvas/`. Then grill me one question at a time on: card density at 30 cards, connector shape, whether elevation reads, what a chip looks like, the drag-from-card gesture. Record the Answer on ticket 17 as addenda to tickets 05, 11, 12, 18 where it changes them. Do not touch any other ticket. Plain industry terms only.

## Step 3 · Prototype 16 (Explainer card inline and on the Canvas; the offer decision)

/wayfinder .scratch/surfaces/map.md ticket 16

Claim ticket 16. Read the Answer blocks of tickets 02, 03, 14, 15 and the prototype 17 result in full. Run `impeccable shape` first for a one-page brief at `docs/gauntlet/briefs/explainer.md`, then `/prototype`: a throwaway HTML file in the material system showing (1) the Reader: "Attention Is All You Need" §3.2 reflowed at Newsreader 18/1.55 on a 68-character measure, with a stub explainer card opening below the scaled dot-product passage (a looping SVG or canvas animation standing in for the Manim clip, a timeline you can scrub, the caption line "Explains: Attention is all you need · §3.2, sentences 4–7" that highlights the sentences on hover, the follow-up field, Try again with a hint, Export), and the long-wait loading state with stage text before it appears; (2) the same explainer as a card on the Canvas from prototype 17, attached to its passage's question card; (3) three variants of how the AI could signal an explainer is available for a passage: a small cold icon in the margin, a row in the right sidebar's details panel, and no signal at all until asked. Screenshot all states into `docs/gauntlet/prototypes/explainer/`. Then grill me one question at a time on: below-only reflow, card size, scrub control, how it closes, how the caption reads, how it reads on the Canvas, and which offer treatment, if any, is acceptable. The offer decision closes the deferred part of ticket 02: write its Answer as an addendum there. Record the rest on ticket 16 with addenda to 14 and 15 where changed. Plain industry terms only.

## Step 4 · Task 27 (assemble SURFACES.md, AFK)

/wayfinder .scratch/surfaces/map.md ticket 27

Claim ticket 27. Every other ticket must be resolved; if one is not, stop and say which. Read every ticket's Answer block including all addenda, and CONTEXT.md. Write `docs/gauntlet/SURFACES.md`: a preamble carrying the cross-cutting rules verbatim (the five-pains table from 01, the AI verb table and human-only verbs from 02 with its addenda, the state grammar from 03, the keyboard grammar from 04 with addenda, the event table from 26); then one section per surface in the roster fixed by ticket 13's addendum: Shell, Opening screen (with the question door as a state, from 20 and 23), Reader (14 and 22), Canvas (05, 11, 12, 18, 21, and prototype 17), Playbook (24), Settings (25); then components: Explainer card (15 and prototype 16), Pointing assistant (19), ⌘K finder (from 13). Each section states the nine fields in this order and with these headings: Job; How you arrive; Primary flow to the moment of understanding; States (empty, loading, partial, failed, stale, unsupported, each with copy and recovery or n/a with reason); AI may / Only the human; Keyboard; Reference bar; Event log (writes / reads). Where an addendum supersedes earlier text, write only the final state; never both. Add no decision that is not in a ticket; if a field cannot be filled from the tickets, write "OPEN:" with the question and list all OPEN items at the top. Use plain industry terms; translate any metaphor from older docs once. Finish with a checklist table: surface × nine fields, ticked. Then set ticket 27 resolved, add its line to the map's Decisions so far, and report the OPEN count.
